apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helmkubechart.fullname" . }}-test-connection"
  labels:
    {{- include "helmkubechart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "helmkubechart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
