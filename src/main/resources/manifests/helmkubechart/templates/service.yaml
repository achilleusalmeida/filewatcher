apiVersion: v1
kind: Service
metadata:
  name: {{ include "helmkubechart.fullname" . }}
  labels:
    {{- include "helmkubechart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helmkubechart.selectorLabels" . | nindent 4 }}
